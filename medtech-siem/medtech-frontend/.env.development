# Local (npm run dev) : URL vide => requêtes relatives, relayées par le proxy
# Vite vers localhost:3000 (voir vite.config.ts). Pas de CORS.
# La prod utilise .env.production (VITE_API_URL=https://api.medtech-security.tech).
VITE_API_URL=
